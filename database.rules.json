{
  "rules": {
    "admins": {
      ".read": "auth != null",
      ".write": false
    },
    "users": {
      "$uid": {
        ".read": "auth != null && ($uid === auth.uid || root.child('admins').child(auth.uid).exists())",
        ".write": "auth != null && $uid === auth.uid",
        "profile": {
            ".validate": "newData.hasChildren(['email', 'disabled'])"
        }
      }
    }
  }
}
